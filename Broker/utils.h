#include "includes.h"

int calcularBytes (pedido){ //que tipo es el pedido? como lo recibo y como lo desarmo?

    int bytes = 0;

    switch(pedido->operacion){
			case NEW_POKEMON:
				puts(" Recibi un NEW POKEMON");
				
                //bytes = ;

				return bytes;
			case :
				puts(" Recibi un ");

                //bytes = ;

				return bytes;
			case :
				puts(" Recibi un ");

                //bytes = ;

				return bytes;
			case :
				puts(" Recibi un ");

                //bytes = ;

				return bytes;
			case :
				puts(" Recibi un ");

                //bytes = ;

				return bytes;
			case :
				puts(" Recibi un ");

                //bytes = ;

				return bytes;
			default:
				break;
		}


    return bytes;
}